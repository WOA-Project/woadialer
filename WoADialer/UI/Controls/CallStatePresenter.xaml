<UserControl
    x:Class="WoADialer.UI.Controls.CallStatePresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WoADialer.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="32"
    d:DesignWidth="128">

    <UserControl.Resources>
        <ResourceDictionary Source="..\Conventers\Conventers.xaml"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="32"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <FontIcon Glyph="{x:Bind Path=PresentedCall.State, Mode=OneWay, Converter={StaticResource CallStateToIconString}, ConverterParameter=PresentedCall.StateReason}"/>
        <TextBlock HorizontalAlignment="Stretch" VerticalAlignment="Center" HorizontalTextAlignment="Left" Text="{x:Bind Path=PresentedCall.State, Mode=OneWay, Converter={StaticResource CallStateToTextString}, ConverterParameter=PresentedCall.StateReason}" Grid.Column="1" />
    </Grid>
</UserControl>
